<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Init-IT Association</title>
    <style>
        /* Ensure mobile nav links are styled and visible */
      #mobileNav {
    transform: translateX(100%); /* Start off-screen to the right */
    transition: transform 0.3s ease;
    top: 0; /* Aligns with the top of the viewport */
    right: 0; /* Aligns with the right side of the viewport */
    height: 100vh; /* Ensures it takes the full height of the viewport */
    width: 75%; /* Adjust the width as needed */
    padding: 0; /* Remove any default padding */
    margin: 0; /* Remove any default margin */
    background-color: rgba(0, 0, 0, 0.8); /* Semi-transparent background */
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: 1.5rem;
}

        #mobileNav a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        #hamburger {
            z-index: 60;
        }

        /* Mobile Nav initial hidden state */
        #mobileNav {
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            top: 0;
            /* Ensure it is aligned with the top */
            left: 0;
            /* Align with the left side */
        }

        /* Mobile Nav active state */
        #mobileNav.active {
            transform: translateX(0);
        }

#closeMobileNav {
    margin: 1rem; /* Adjust margin if necessary */
    align-self: flex-end; /* Align to the end of the container */
}
        @import url('https://fonts.googleapis.com/css2?family=Righteous&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Lato:wght@300&display=swap');

        /* General styling */
        body {
            background: linear-gradient(45deg, rgba(0, 212, 255, 1) 0%, rgba(11, 3, 45, 1) 100%);
            background-image: url('https://images.unsplash.com/photo-1619204715997-1367fe5812f1?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1889&q=80');
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Navbar scroll effect */
        nav.scrolled {
            background-color: rgba(0, 0, 0, 0.8);
            transition: background-color 0.3s ease;
        }

        /* Container for events */
        .events-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            max-width: 1200px;
            margin: 0 auto;
            margin-top: 55px;
        }

        /* Individual event styling */
        .container {
            backdrop-filter: blur(16px) saturate(180%);
            -webkit-backdrop-filter: blur(16px) saturate(180%);
            background-color: rgba(17, 25, 40, 0.75);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.125);
            padding: 20px;
            width: 350px;
            filter: drop-shadow(0 30px 10px rgba(0, 0, 0, 0.125));
            text-align: center;
            box-sizing: border-box;
        }

        .wrapper {
            width: 100%;
        }

        .banner-image {
            background-position: center;
            background-size: cover;
            height: 200px;
            width: 100%;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.255);
        }

        h1 {
            font-family: 'Righteous', sans-serif;
            color: rgba(255, 255, 255, 0.98);
            text-transform: uppercase;
            font-size: 1.8rem;
            margin-top: 15px;
        }

        p {
            color: #fff;
            font-family: 'Lato', sans-serif;
            text-align: center;
            font-size: 0.8rem;
            line-height: 150%;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            margin-top: 10px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .button-wrapper {
            margin-top: 20px;
        }

        .btn {
            border: none;
            padding: 10px 20px;
            border-radius: 20px;
            font-size: 0.8rem;
            letter-spacing: 1.5px;
            cursor: pointer;
            text-transform: uppercase;
        }

        .btn+.btn {
            margin-left: 10px;
        }

        .outline {
            background: transparent;
            color: rgba(0, 212, 255, 0.9);
            border: 1px solid rgba(0, 212, 255, 0.6);
            transition: all .3s ease;
        }

        .outline:hover {
            transform: scale(1.125);
            color: rgba(255, 255, 255, 0.9);
            border-color: rgba(255, 255, 255, 0.9);
            transition: all .3s ease;
        }

        .fill {
            background: rgba(0, 212, 255, 0.9);
            color: rgba(255, 255, 255, 0.95);
            filter: drop-shadow(0);
            font-weight: bold;
            transition: all .3s ease;
        }

        .fill:hover {
            transform: scale(1.125);
            border-color: rgba(255, 255, 255, 0.9);
            filter: drop-shadow(0 10px 5px rgba(0, 0, 0, 0.125));
            transition: all .3s ease;
        }
    </style>
</head>

<body>
    <nav class="w-screen bg-transparent h-fit overflow-hidden fixed top-0 left-0 z-50">
    <div class="py-4 lg:px-8 px-4 max-w-[1280px] h-16 m-auto text-white flex items-center justify-between">
        <div class="lg:w-1/2">
            <a href="/" class="lg:text-2xl text-xl uppercase tracking-wider cursor-pointer font-bold">Init()</a>
        </div>
        <div class="hidden lg:flex lg:gap-8 gap-6 uppercase tracking-wider cursor-pointer text-lg items-center lg:w-1/2 justify-end"
            id="navItems">
            <a href="/events" class="group">
                EVENTS
                <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
            </a>
            <a href="/members" class="group">
                MEMBERS
                <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
            </a>
            <a href="/contactus" class="group">
                CONTACT
                <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
            </a>
            <a href="/about" class="group">
                ABOUT US
                <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
            </a>
        </div>
        <button id="hamburger" class="lg:hidden text-white text-2xl focus:outline-none">
            <i class="fas fa-bars"></i>
        </button>
    </div>
    <div id="mobileNav"
        class="fixed inset-y-0 right-0 w-[75%] bg-gray-800 text-white flex flex-col items-center gap-6 pt-16 px-4 transition-transform duration-300 ease-in-out">
        <button class="self-end text-2xl" id="closeMobileNav">
            <i class="fas fa-times"></i>
        </button>
        <a href="/events">EVENTS</a>
        <a href="/members">MEMBERS</a>
        <a href="/contactus">CONTACT</a>
        <a href="/about">ABOUT US</a>
    </div>
</nav>

    <div class="events-container">
        {{#each events}}
        <div class="container">
            <div class="wrapper">
                <!-- Use the route to fetch the image from the database -->
                <div class="banner-image" style="background-image: url('/image/{{this._id}}');"> </div>
                <h1>{{this.title}}</h1>
                <p>{{this.description}}</p>
            </div>
            <div class="button-wrapper">
                <a href="/event/{{this._id}}" class="btn outline">DETAILS</a>
            </div>
        </div>
        {{/each}}
    </div>

    <script>
        // Handle navbar background color change on scroll
        const navbar = document.querySelector('nav');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Handle hamburger menu toggle
       // Handle hamburger menu toggle
document.getElementById('hamburger').addEventListener('click', function () {
    document.getElementById('mobileNav').classList.add('active');
});

document.getElementById('closeMobileNav').addEventListener('click', function () {
    document.getElementById('mobileNav').classList.remove('active');
});

    </script>
</body>

</html>