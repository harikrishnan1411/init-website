<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #272727;
            color: #ffffff;
        }

        .sidebar {
            margin: 0;
            padding: 0;
            width: 200px;
            background-color: #1f1f1f;
            position: fixed;
            height: 100%;
            overflow: auto;
        }

        .sidebar a {
            display: block;
            color: #ffffff;
            padding: 16px;
            text-decoration: none;
        }

        .sidebar a.active {
            background-color: #04AA6D;
            color: white;
        }

        .sidebar a:hover:not(.active) {
            background-color: #026e47;
            color: white;
        }

        div.content {
            margin-left: 200px;
            padding: 1px 16px;
            height: 1000px;
        }

        .card {
            background-color: #2b2b2b;
            color: #ffffff;
            border: none;
        }

        .btn-primary {
            background-color: #343a40;
            border-color: #343a40;
        }

        .btn-primary:hover {
            background-color: #575757;
            border-color: #575757;
        }

        table {
            background-color: #2b2b2b;
            color: #ffffff;
            width: 100%;
            margin-bottom: 1rem;
        }

        th {
            background-color: #343a40;
            color: #ffffff;
        }

        td {
            background-color: #2b2b2b;
            color: #ffffff;
        }

        table tr:hover {
            background-color: #3a3a3a;
        }

        table th,
        table td {
            border: 1px solid #444;
            padding: 0.75rem;
            vertical-align: top;
            border-top: 1px solid #4d4d4d;
        }

        @media screen and (max-width: 700px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }

            .sidebar a {
                float: left;
            }

            div.content {
                margin-left: 0;
            }
        }

        @media screen and (max-width: 400px) {
            .sidebar a {
                text-align: center;
                float: none;
            }
        }
    </style>
</head>

<body>
    <div class="container-fluid">
        <!-- The sidebar -->
        <div class="sidebar">
            <a href="/admin">Dashboard</a>
            <a href="/admin/eventManagement">Event Management</a>
            <a href="/admin/memberManagement">Member Management</a>
            <a class = "active" href="/admin/userFeedbacks">User Feedbacks</a>
            <a href="/admin/logout">Logout</a>
        </div>

        <div class="content">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                {{#each Messages}}
                <div class="bg-black rounded-lg shadow-lg p-6">
                    <h2 class="text-xl font-semibold mb-4">Email: {{email}}</h2>
                    <p class="text-gray-600">Subject: {{subject}}</p>
                    <p class="text-gray-600">Message: {{message}}</p>
                    <div class="mt-4 flex justify-between items-center">
                        <span class="text-gray-500">Date: {{date}}</span>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
    <script>
        

    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>