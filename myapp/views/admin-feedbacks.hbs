<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #272727;
            color: #ffffff;
            margin: 0;
            padding: 0;
        }

        .sidebar {
            margin-left: -12px;
            width: 200px;
            background-color: #1f1f1f;
            position: fixed;
            height: 100%;
            overflow: auto;
        }

        .sidebar a {
            display: block;
            color: #ffffff;
            padding: 16px;
            text-decoration: none;
        }

        .sidebar a.active {
            background-color: #04AA6D;
            color: white;
        }

        .sidebar a:hover:not(.active) {
            background-color: #026e47;
            color: white;
        }

        div.content {
            margin-left: 200px;
            padding: 1px 16px;
            height: 1000px;
        }


        .btn-primary {
            background-color: #343a40;
            border-color: #343a40;
        }

        .btn-primary:hover {
            background-color: #575757;
            border-color: #575757;
        }


        @media screen and (max-width: 700px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }

            .sidebar a {
                float: left;
            }

            div.content {
                margin-left: 0;
            }
        }

        @media screen and (max-width: 400px) {
            .sidebar a {
                text-align: center;
                float: none;
            }
        }
        .card-body.bg-dark {
    background-color: #1a1a1a; /* Dark background for cards */
}
.btn-outline-light {
    border-color: #f8f9fa; /* Light outline for buttons */
    color: #f8f9fa; /* Button text color */
}
.btn-outline-light:hover {
    background-color: #f8f9fa;
    color: #212529; /* Dark text on hover */
}
.text-muted {
    color: #6c757d !important; /* Muted gray text */
}
.text-light {
    color: #f8f9fa !important; /* Light text */
}

    </style>
</head>

<body>
    <div class="container-fluid">
        <!-- The sidebar -->
        <div class="sidebar">
            <a href="/admin">Dashboard</a>
            <a href="/admin/eventManagement">Event Management</a>
            <a href="/admin/memberManagement">Member Management</a>
            <a class="active" href="/admin/userFeedbacks">User Feedbacks</a>
            <a href="/admin/logout">Logout</a>
        </div>

        <div class="container py-5">
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                {{#each Messages}}
                <div class="col">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body p-4 bg-dark text-white">
                            <h6 class="text-muted mb-2">From:</h6>
                            <h5 class="card-title text-light">{{email}}</h5>
                            <hr class="text-muted">
                            <p class="text-muted mb-1"><strong>Subject:</strong> {{subject}}</p>
                            <p class="text-muted mb-3"><strong>Message:</strong> {{message}}</p>
                        </div>
                        <div class="card-footer bg-dark border-0 d-flex justify-content-between align-items-center">
                            <small class="text-muted">Received on: {{date}}</small>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>