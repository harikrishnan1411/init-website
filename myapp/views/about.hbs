<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Kode+Mono:wght@400..700&display=swap"
        rel="stylesheet">
    <title>About Us - init() IT Association</title>
    <style>
        /* General Styles */
         html {
            color-scheme: dark;
            /* or use 'dark' if you want your website to always be dark themed */
        }
         nav,
        {
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }
        main {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 20px;
        }

        section {
            padding: 50px 20px;
        }

        /* Fade-in animation */
        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Mobile Nav */
        #mobileNav {
            transform: translateX(100%);
            /* Start off-screen to the right */
            transition: transform 0.3s ease;
            top: 0;
            /* Aligns with the top of the viewport */
            right: 0;
            /* Aligns with the right side of the viewport */
            height: 100vh;
            /* Ensures it takes the full height of the viewport */
            width: 100%;
            /* Adjust the width as needed */
            padding: 0;
            /* Remove any default padding */
            margin: 0;
            /* Remove any default margin */
            background-color: rgba(0, 0, 0, 0.8);
            /* Semi-transparent background */
            display: flex;
            flex-direction: column;
            align-items: center;
            font-size: 1.5rem;
            z-index: 9999;
        }

        #mobileNav a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        #mobileNav.active {
            transform: translateX(0);
        }

        #mobileNav {
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            top: 0;
            /* Ensure it is aligned with the top */
            left: 0;
            /* Align with the left side */
        }

        /* Mobile Nav active state */
        #mobileNav.active {
            transform: translateX(0);
        }

        #closeMobileNav {
            margin: 1rem;
            /* Adjust margin if necessary */
            align-self: flex-end;
            /* Align to the end of the container */
        }

        /* Navbar scroll effect */
        nav.scrolled {
            background-color: rgba(0, 0, 0, 0.8);
            transition: background-color 0.3s ease;
        }

        /* Section Styles */
        .section-title {
            text-align: center;
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 30px;
            color: #333;
        }

        .section-title span {
            color: #6366F1;
            border-bottom: 2px solid #6366F1;
            padding-bottom: 5px;
        }

        .section-description {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
            color: #555;
            margin-bottom: 40px;
        }

        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            align-items: center;
        }

        .card {
            background-color: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
        }

        .card img {
            width: 100%;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .card h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        .card p {
            color: #666;
        }

        /* Call to Action Section */
        .cta {
            background-color: #6366F1;
            color: white;
            padding: 60px 30px;
            text-align: center;
            border-radius: 10px;
            margin: 40px 0;
        }

        .cta h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .cta p {
            font-size: 1.2rem;
            margin-bottom: 30px;
        }

        .cta button {
            background-color: white;
            color: #6366F1;
            padding: 10px 20px;
            border-radius: 30px;
            font-weight: bold;
            font-size: 1.1rem;
            transition: background-color 0.3s ease;
        }

        .cta button:hover {
            background-color: #E0E7FF;
        }

        
    </style>
</head>

<body class="bg-gray-900">

    <nav class="w-full bg-transparent h-fit overflow-hidden fixed top-0 left-0 z-50">
        <div class="py-4 lg:px-8 px-4 max-w-[1920px] h-16 mx-auto text-white flex items-center justify-between">
            <div class="lg:w-1/2">
                <a href="/" class="lg:text-2xl text-xl uppercase tracking-wider cursor-pointer font-bold">
                    <img src="/images/logo.jpg" alt="Company Logo" class="h-12 object-contain">
                </a>
            </div>
            <div class="hidden lg:flex lg:items-center lg:justify-end lg:flex-1 lg:gap-8 uppercase tracking-wider cursor-pointer text-lg"
                id="navItems">
                <a href="/events" class="group">
                    EVENTS
                    <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
                </a>
                <a href="/members" class="group">
                    MEMBERS
                    <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
                </a>
                <a href="/contactus" class="group">
                    CONTACT
                    <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
                </a>
                <a href="/about" class="group">
                    ABOUT US
                    <div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div>
                </a>
            </div>
            <button id="hamburger" class="lg:hidden text-white text-2xl focus:outline-none">
                <i class="fas fa-bars"></i>
            </button>
        </div>
        <div id="mobileNav"
            class="fixed inset-0 top-16 left-[-100%] w-[100%] bg-gray-800 text-white flex flex-col items-center gap-6 pt-16 px-4 transition-transform duration-300 ease-in-out">
            <button class="self-end text-2xl" id="closeMobileNav">
                <i class="fas fa-times"></i>
            </button>
            <a href="/events">EVENTS</a>
            <a href="/members">MEMBERS</a>
            <a href="/contactus">CONTACT</a>
            <a href="/about">ABOUT US</a>
        </div>
    </nav>
    <!-- About Section -->
    <section>
        <div class="max-w-screen-xl mx-auto">
            <div class="sm:flex items-center">
                <div class="sm:w-1/2 p-10 fade-in">
                    <img src="https://i.imgur.com/WbQnbas.png" alt="About Us">
                </div>
                <div class="sm:w-1/2 p-5 fade-in">
                    <span class="text-gray-500 border-b-2 border-indigo-600 uppercase">About us</span>
                    <h2 class="my-4 font-bold text-3xl sm:text-4xl">
                        <span class="text-indigo-600">Our Association</span>
                    </h2>
                    <p class="text--700 text-white">
                        We are an association focused on promoting innovation and excellence in the IT and AD
                        department.
                        Our mission is to foster collaboration, create opportunities, and support our members in their
                        professional growth.
                        From conferences to tech talks to tech events, we bring the best minds together to shape the
                        future of IT.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Navbar scroll effect
        const navbar = document.querySelector('nav');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Mobile Nav Toggle
        document.getElementById('hamburger').addEventListener('click', function () {
            document.getElementById('mobileNav').classList.add('active');
        });

        document.getElementById('closeMobileNav').addEventListener('click', function () {
            document.getElementById('mobileNav').classList.remove('active');
        });
        // Fade-in animation on scroll
        const fadeIns = document.querySelectorAll('.fade-in');

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        });

        fadeIns.forEach(fadeIn => {
            observer.observe(fadeIn);
        });
    </script>
</body>

</html>