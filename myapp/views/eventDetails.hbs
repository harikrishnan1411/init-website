<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Kode+Mono:wght@400..700&display=swap"
        rel="stylesheet">
    <title>Init-IT Association</title>
    <style>
        @media (prefers-color-scheme: light) {
            html {
                color-scheme: dark;
                /* Force dark mode */
            }
        }

        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Roboto+Slab:wght@400;700&display=swap');

        html {
            color-scheme: dark;
            /* or use 'dark' if you want your website to always be dark themed */
        }

        nav {
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }

        /* General styling */
        body {
            font-family: 'Poppins', sans-serif;

        }

        /* Mobile Nav */


        /* Ensure mobile nav links are styled and visible */
        #mobileNav {
            transform: translateX(100%);
            /* Start off-screen to the right */
            transition: transform 0.3s ease;
            top: 0;
            /* Aligns with the top of the viewport */
            right: 0;
            /* Aligns with the right side of the viewport */
            height: 100vh;
            /* Ensures it takes the full height of the viewport */
            width: 100%;
            /* Adjust the width as needed */
            padding: 0;
            /* Remove any default padding */
            margin: 0;
            /* Remove any default margin */
            background-color: rgba(0, 0, 0, 0.8);
            /* Semi-transparent background */
            display: flex;
            flex-direction: column;
            align-items: center;
            font-size: 1.5rem;
            z-index: 9999;
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            top: 0;
            /* Ensure it is aligned with the top */
            left: 0;
        }

        #mobileNav a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        #mobileNav.active {
            transform: translateX(0);
        }


        /* Mobile Nav active state */
        #mobileNav.active {
            transform: translateX(0);
        }

        #closeMobileNav {
            margin: 1rem;
            /* Adjust margin if necessary */
            align-self: flex-end;
            /* Align to the end of the container */
        }

       

        /* Event Details Section */

        /* Event Image Styling */
        .event-image {
            background-position: center;
            background-size: cover;
            height: 500px;
            width: 100%;
            margin-bottom: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }


        /* Event Title Styling */
        .event-details h1 {
            
            
            margin-bottom: 20px;
            margin-left: 10px;
            text-transform: uppercase;
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
            margin-top: 15px;
        }

        /* Event Info */
        .event-info {
            margin-left: 10px;
            margin-bottom: 30px;
            color: #d4d4d4;
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }

        .event-info p {
            margin-left: 10px;
            font-size: 1.2rem;
            line-height: 1.8;
            margin: 10px 0;
        }

        /* Coordinators List */
        .event-coordinators p {
            margin-left: 10px;
            font-size: 1.2rem;
            font-weight: 600;
            color: #e6e6e6;
            margin-bottom: 10px;
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }

        .event-coordinators ul {
            margin-left: 10px;
            list-style-type: disc;
            padding-left: 20px;
        }

        .event-coordinators li {
            margin-left: 10px;
            font-size: 1.1rem;
            color: #dedede;
            margin: 5px 0;
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }

        /* Button Styling */
        .btn {
            margin-left: 10px;
            display: inline-block;
            padding: 15px 30px;
            font-size: 1rem;
            text-transform: uppercase;
            color: white;
            background-color: #3182ce;
            border-radius: 30px;
            transition: background-color 0.3s ease;
            text-align: center;
            font-family: "Kode Mono", monospace;
            font-optical-sizing: auto;
            font-weight: <weight>;
            font-style: normal;
        }

        .btn:hover {
            background-color: #2b6cb0;
        }

        section {
            margin-top: 20px;
            z-index: 10;
        }

        /*Start Animations*/
        @-webkit-keyframes animatetop {
            from {
                top: -300px;
                opacity: 0;
            }

            to {
                top: 0;
                opacity: 1;
            }
        }

        @keyframes animatetop {
            from {
                top: -300px;
                opacity: 0;
            }

            to {
                top: 0;
                opacity: 1;
            }
        }

        @-webkit-keyframes zoomIn {
            0% {
                opacity: 0;
                -webkit-transform: scale3d(0.3, 0.3, 0.3);
                transform: scale3d(0.3, 0.3, 0.3);
            }

            50% {
                opacity: 1;
            }
        }

        @keyframes zoomIn {
            0% {
                opacity: 0;
                -webkit-transform: scale3d(0.3, 0.3, 0.3);
                transform: scale3d(0.3, 0.3, 0.3);
            }

            50% {
                opacity: 1;
            }
        }

        /*End Animations*/
        section{
            margin-top: 10px;
        }

    </style>
</head>

<body class="bg-gray-900">
    <nav class="w-full bg-transparent h-fit fixed top-0 left-0 z-50 mb-20">
        <div class="py-4 lg:px-8 px-4 max-w-[4120px] h-16 mx-auto text-white flex items-center justify-between">
            <div class="lg:w-1/2">
                <a href="/" class="lg:text-2xl text-xl uppercase tracking-wider cursor-pointer font-bold">
                    <img src="/images/logo.jpg" alt="Company Logo" class="h-12 object-contain">
                </a>
            </div>
            <div class="hidden lg:flex lg:items-center lg:justify-end lg:flex-1 lg:gap-8 uppercase tracking-wider cursor-pointer text-lg" id="navItems">
                <a href="/events" class="group">EVENTS<div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div></a>
                <a href="/members" class="group">MEMBERS<div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div></a>
                <a href="/contactus" class="group">CONTACT<div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div></a>
                <a href="/about" class="group">ABOUT US<div class="w-0 group-hover:w-full h-0.5 bg-white ease-in-out duration-500"></div></a>
            </div>
            <button id="hamburger" class="lg:hidden text-white text-2xl focus:outline-none">
                <i class="fas fa-bars"></i>
            </button>
        </div>
        <div id="mobileNav" class="fixed inset-0 top-16 left-[-100%] w-[100%] bg-gray-800 text-white flex flex-col items-center gap-6 pt-16 px-4 transition-transform duration-300 ease-in-out">
            <button class="self-end text-2xl" id="closeMobileNav">
                <i class="fas fa-times"></i>
            </button>
            <a href="/events">EVENTS</a>
            <a href="/members">MEMBERS</a>
            <a href="/contactus">CONTACT</a>
            <a href="/about">ABOUT US</a>
        </div>
    </nav>
    <br>
    <main>
        <section>
            <div class="event-details max-w-screen-lg mx-auto mt-12 px-5 lg:flex lg:gap-8">
                <div class="lg:w-1/2">
                    <div class="w-full h-screen/2 rounded-lg overflow-hidden flex items-center justify-center">
                        <img src="/image/{{event._id}}" alt="Event Image" class="w-auto h-full object-contain object-center">
                    </div>
                </div>
                <div class="lg:w-1/2 flex flex-col justify-center">
                    <h1 class="text-white text-4xl lg:text-5xl font-bold mt-4 lg:mt-0">{{event.title}}</h1>
                    <div class="event-info text-white mt-2">
                        <p><em>{{event.tagline}}</em></p>
                        <p>{{event.description}}</p>
                        <p><strong>Venue:</strong> {{event.venue}}</p>
                        <p><strong>Date:</strong> {{event.date}}</p>
                        <p><strong>Registration Fees:</strong> {{event.fees}}</p>
                    </div>
                    <div class="event-coordinators text-white mt-2">
                        <p><strong>Coordinators:</strong></p>
                        <ul>
                            {{#each event.coordinators}}
                            <li>{{this.name}} - {{this.number}}</li>
                            {{/each}}
                        </ul>
                    </div>
                    {{#unless event.completed}}
                    <a href="{{event.formLink}}" class="btn mt-8">Join</a>
                    {{/unless}}
                    {{#if event.completed}}
                    <p class="text-red-500 mt-8">Event has ended</p>
                    {{/if}}
                </div>
            </div>
        </section>
    </main>

    <script>
        // Navbar background change on scroll
        const navbar = document.querySelector('nav');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Handle hamburger menu toggle
        document.getElementById('hamburger').addEventListener('click', function () {
            document.getElementById('mobileNav').classList.add('active');
        });

        document.getElementById('closeMobileNav').addEventListener('click', function () {
            document.getElementById('mobileNav').classList.remove('active');
        });
    </script>
</body>


</html>